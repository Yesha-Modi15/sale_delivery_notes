<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_delivery_notes" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.delivery.notes</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='other_information']//group[@name='sale_shipping']" position="inside">
                <field name="delivery_notes" placeholder="Add delivery notes..."/>
            </xpath>
        </field>
    </record>

    <record id="view_order_form_inherit_location" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.location</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='partner_details']/field[@name='partner_shipping_id']" position="after">
                <field name="location_id" readonly="state not in ['draft', 'sent']"/>
            </xpath>
        </field>
    </record>
</odoo>
